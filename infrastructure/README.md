# Infrastructure

Local Docker Compose definitions, NATS configuration, datastore bootstrap, observability configuration, packaging, backup, and restore assets will live here. Infrastructure must bind locally, use pinned versions, and operate without automatic external communication.

