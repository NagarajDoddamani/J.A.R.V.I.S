# Backend

Future Python 3.12 services and agents live here. Each deployable follows clean/hexagonal architecture and owns its persistence schema, configuration, tests, and adapters. No production implementation exists yet.

Expected top-level areas after Phase 01: `services/`, `agents/`, and narrowly scoped shared backend libraries.

